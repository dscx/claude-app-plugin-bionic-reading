---
title: Front matter stays put
tag: v2
---

# A heading is prose

Bionic reading makes words easier to scan. Run `npm install` first, then open
./src/app.js and set API_KEY in .env. See https://example.com/docs?a=1 and the
[plugin docs](https://code.claude.com/docs/en/plugins) for more.

- A list item with **already bold** text and *italics*
- A path like ~/Documents/Dev and a file config.json
- Numbers 42, versions 1.2.3, and an em dash — untouched

> A blockquote is prose too.

| Column | Meaning |
| ------ | ------- |
| left   | right   |

```js
// this whole block must survive byte for byte
const word = "reading";
```

    indented code stays code

Don't split contractions. HTTP and API stay whole. A twelve-letter word like
extraordinary gets five. <span class="x">markup survives</span> &amp; entities.
